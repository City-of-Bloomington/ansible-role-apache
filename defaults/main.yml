---
apache_serveradmin: example@domain.com
apache_default_domain: "{{ ansible_host }}"

apache_ssl:
  key:
    file: server.key
  cert:
    file: server.crt
    subj: "/CN={{ apache_default_domain }}"
...
